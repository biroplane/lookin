html
  head
    title= title
    script.
      window.nodeRequire = require;
      delete window.require;
      delete window.exports; delete window.module;
    script(src='/js/pace.min.js')
    script(src='https://code.jquery.com/jquery-3.1.1.min.js',type='text/javascript')

    script.
      //window.$ = window.jQuery = require("jquery").remote;
      $().ready(function(){
        console.log("READY")
        alert("HELOOOOOOOOOOOOOOOOO")
      });
      var fs=require("fs")
  body
  each item in rows
    li
    each line in item
      span= line
